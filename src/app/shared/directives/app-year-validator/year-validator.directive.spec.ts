import {YearValidatorDirective} from './year-validator.directive';

describe('YearValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new YearValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
