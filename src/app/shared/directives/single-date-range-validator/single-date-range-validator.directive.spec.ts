import {SingleDateRangeValidatorDirective} from './single-date-range-validator.directive';

describe('SingleDateRangeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SingleDateRangeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
